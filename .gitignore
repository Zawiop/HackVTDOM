# Secrets
.env
.env.*
!.env.example
*.env
backend/.env
frontend/.env

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp

# Python
__pycache__/
*.py[cod]
.venv/
.venv*/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/

# Node
node_modules/
dist/
build/
.vite/
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Local dev database (SQLite fallback when SUPABASE_URL is unset)
*.db
*.sqlite3
backend/local.db

# Logs / caches
*.log
.cache/

# Model weights / generated assets
*.ckpt
*.safetensors
*.pt
*.pth
output/
outputs/
tmp/


!**/placeholder.glb

# TS incremental build info
*.tsbuildinfo

# Local placement debug render (scripts/placement_preview.py)
backend/placement-preview.html

# Runtime undo stash (app/store/trash.py). Rewritten on every delete and it
# holds whatever rows were last removed, so committing it both churns the
# history and puts one machine's world state in everyone else's checkout.
backend/trash.json
**/trash.json
.vercel
.env*
