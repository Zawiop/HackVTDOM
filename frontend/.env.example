# Copy to frontend/.env and fill in. NEVER commit the real .env.
#
# The frontend talks to the backend through Vite's /api proxy, so these are only
# needed if a component queries Supabase directly (nothing does today).
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
