import type { ReactNode } from "react";

/** Types for MapShell.jsx, which is plain JSX and not typechecked itself. */
declare function MapShell(props: { entrySlot?: ReactNode }): JSX.Element;

export default MapShell;
